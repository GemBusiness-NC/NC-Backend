version: '3.8'

services:
  backend:
    build: .
    container_name: gem_backend
    image: gem-backend
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
